import React from 'react';
import styles from './SessionSidebar.module.css';

const SessionSidebar = () => (
  <aside className={styles.sidebar}>
    <p>Session sidebar will show stacked services + totals.</p>
  </aside>
);

export default SessionSidebar;
