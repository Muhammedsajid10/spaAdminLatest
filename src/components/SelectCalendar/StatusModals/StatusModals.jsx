import React from 'react';
import styles from './StatusModals.module.css';

const StatusModals = () => (
  <div className={styles.wrapper}>
    <p>Status modals (conflict/unavailable) will be ported here.</p>
  </div>
);

export default StatusModals;
