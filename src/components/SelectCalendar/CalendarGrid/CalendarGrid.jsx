import React from 'react';
import styles from './CalendarGrid.module.css';

const CalendarGrid = () => (
  <section className={styles.grid}>
    <p>Calendar grid preview placeholder.</p>
  </section>
);

export default CalendarGrid;
