import React from 'react';
import styles from './BookingFlow.module.css';

const BookingFlow = () => (
  <div className={styles.flow}>
    <p>Booking flow will be extracted here (service, professional, client steps).</p>
  </div>
);

export default BookingFlow;
